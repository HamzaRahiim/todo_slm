[supervisord]
nodaemon=true

[program:ollama]
command=bash -c "curl -fsSL https://ollama.ai/install.sh | sh && ollama serve"
autorestart=true
priority=1

[program:app]
command=python main.py
autorestart=true
priority=2
